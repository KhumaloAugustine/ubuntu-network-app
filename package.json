{
  "name": "ubuntu-network-app",
  "version": "0.1.0",
  "description": "Safety-first community connection platform",
  "private": true,
  "license": "MIT",
  "author": "Augustine Khumalo",
  "workspaces": ["api", "mobile"],
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "scripts": {
    "postinstall": "npm run setup",
    "setup": "echo 'Workspace setup complete'",
    "lint": "npm run lint --workspaces",
    "format": "npm run format --workspaces",
    "test": "npm run test --workspaces",
    "build": "npm run build --workspaces"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^6.0.0",
    "@typescript-eslint/parser": "^6.0.0",
    "eslint": "^8.0.0",
    "prettier": "^3.0.0",
    "typescript": "^5.4.2"
  }
}
