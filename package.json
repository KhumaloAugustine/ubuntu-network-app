{
  "name": "ubuntu-network-app",
  "private": true,
  "version": "0.1.0",
  "workspaces": ["mobile", "api"],
  "scripts": {
    "install": "npm install",
    "build": "echo \"Build step - run per-package\""
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^6.0.0",
    "@typescript-eslint/parser": "^6.0.0",
    "eslint": "^8.0.0"
  }
}
